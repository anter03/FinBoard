import { CheckResultLogService } from './check-result-log-service';

describe('CheckResultLogService', () => {
  it('should create an instance', () => {
    expect(new CheckResultLogService()).toBeTruthy();
  });
});
